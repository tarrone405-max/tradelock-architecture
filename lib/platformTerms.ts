// TradeLock's own Terms of Service version — bump this string whenever the
// content at app/terms/page.tsx materially changes, so newly recorded
// acceptances reflect which text a provider actually agreed to. This is
// separate from the per-provider, client-facing `terms_of_service` table.
export const PLATFORM_TERMS_VERSION = "v1";
